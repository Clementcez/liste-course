{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Wording</th>
                <td>{{ category.wording }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_index') }}" class="btn btn-warning"><i class="fas fa-arrow-left"></i> back to list</a>

    <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}" class="btn btn-primary"><i class="fas fa-pen"></i> edit</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
