{% extends 'base.html.twig' %}

{% block title %}ShoppingListItem{% endblock %}

{% block body %}
    <h1>ShoppingListItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shopping_list_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ shopping_list_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_shopping_list_shopping_list_item_index') }}">back to list</a>

    <a href="{{ path('app_admin_shopping_list_shopping_list_item_edit', {'id': shopping_list_item.id}) }}">edit</a>

    {{ include('admin/shopping_list/shopping_list_item/_delete_form.html.twig') }}
{% endblock %}
